<% content_for :header do %>
  <a href='/memos/new' role='button' class='add-button'>追加</a>
<% end %>

<ul>
  <% memos.each do |memo| %>
    <li>
      <a href='/memos/<%= h(memo['id']) %>'>
        <%= h(memo['title']) %>
      </a>
    </li>
  <% end %>
</ul>
